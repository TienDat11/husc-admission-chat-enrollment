# Core ML and NLP libraries (verified working versions)
numpy==2.1.0
sentence-transformers>=3.0.0
rank-bm25==0.2.2
pypdf>=5.0.0
python-docx>=1.1.2

# Web framework  
fastapi>=0.100.0
uvicorn[standard]>=0.24.0

# Data validation and serialization (upgraded to v2 for Python 3.13 compatibility)
pydantic>=2.0.0
pydantic-settings>=2.0.0
jsonschema==4.17.3

# Configuration and data handling
pyyaml==6.0.1
chardet==5.2.0

# Tokenization (try installing without tiktoken first)
# tiktoken==0.4.0

# Logging
loguru==0.7.2

# Testing (optional - comment out if not needed)
# pytest==7.4.3 
# pytest-asyncio==0.21.1

# Development (optional)
black==23.11.0

# Optional dependencies for testing and scripts
httpx==0.25.2
requests==2.31.0

# ========== RAG 2025 UPDATES ==========

# Vector Database
lancedb>=0.20.0
pyarrow>=18.0.0

# LLM Providers
google-generativeai>=0.3.0
groq>=0.4.0
openai>=1.0.0  # For GLM-4 via Z.AI endpoint
zai-sdk>=0.1.0  # GLM-4.5 via Z.AI

# BGE Multi-Vector (AITeamVN) + Qwen3-Embedding (requires >=4.51.0 for Qwen3 support)
transformers>=4.51.0
torch>=2.0.0

# Additional utilities
python-dotenv>=1.0.0
aiohttp>=3.9.0
tenacity>=8.2.0  # For retry logic
tqdm>=4.66.0  # Progress bars

# ========== GRAPH & NLP DEPENDENCIES ==========

# Graph processing for GraphRAG
networkx>=3.0  # Knowledge graph construction and PPR

# Vietnamese NLP (NER service uses underthesea for fallback tokenization)
underthesea>=8.0.0  # Vietnamese word segmentation and NER

# Async file I/O
aiofiles>=23.0.0  # Async file operations for data pipeline

# Nested async support
nest-asyncio>=1.5.0  # Allow nested event loops in Jupyter/scripts

# Machine learning utilities
scikit-learn>=1.0.0  # For evaluation metrics and clustering

# Qdrant vector store (used by BGE retriever & legacy ingest scripts)
qdrant-client>=1.7.0

# Process monitoring (optional, used by benchmark scripts)
psutil>=5.9.0
