Create a clean, professional academic technical diagram on a white background illustrating "Context Fragmentation in Naive RAG". The diagram flows left to right in three columns:

LEFT COLUMN — labeled "Documents (Chunks)" at top: 4 rounded rectangles stacked vertically, colored light orange (#FFE0B2), labeled "Doc A", "Doc B", "Doc C", "Doc D". Draw a dashed orange border around all 4 with a small label "Independent Chunks" above.

MIDDLE COLUMN — labeled "Vector Embeddings" at top: 4 rounded rectangles stacked vertically, colored light green (#C8E6C9), labeled "v_A", "v_B", "v_C", "v_D" (use math-style subscripts). Each has a small grid pattern inside suggesting a vector.

RIGHT COLUMN — labeled "Query" at top: 1 rounded rectangle, colored light yellow (#FFF9C4), labeled "v_q".

ARROWS: Solid dark arrows from each Doc to its corresponding vector (with a small phi symbol on the arrow). Dashed gray arrows from v_q pointing toward v_A, v_B, v_C (but NOT v_D, showing it is missed). A red X icon near v_D showing it is not retrieved.

Add a red annotation text below: "Semantic connections between chunks are lost — no structural link is maintained."

Style: Clean infographic style, sans-serif font (Inter or Helvetica), academic paper quality, 16:9 aspect ratio, 1600x900px. Color palette: orange, green, yellow, white background with subtle gray grid.
