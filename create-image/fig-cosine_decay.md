Create a professional academic line chart on a white background titled "Cosine Similarity Decay in Multi-hop Retrieval".

X-axis: labeled "Number of hops (n)", values 0 to 5, integer ticks.
Y-axis: labeled "Cosine Similarity", range 0.0 to 1.0, with gridlines at 0.2 intervals.

Plot ONE line with filled circle markers:
- Color: deep blue (#1565C0)
- Line style: solid, thick (3px)
- Data points: (0, 1.00), (1, 0.75), (2, 0.52), (3, 0.35), (4, 0.22), (5, 0.13)
- Label in legend: "Naive RAG: similarity decay per hop"
- Each data point has its value annotated directly on the chart

Add a horizontal dashed red line at y=0.3 labeled "Retrieval threshold" to show below which the system fails.
Add a shaded red region below y=0.3 labeled "Retrieval failure zone" in light red (#FFCDD2).

Add annotation arrows: at n=3 pointing to the line with text "Multi-hop queries fail here".

Style: Clean research paper chart style, white background, subtle gray major gridlines, legend in top right corner, axis labels bold, font size readable at print scale. 1400x900px.