Create a professional academic diagram on a white background titled "Text Chunking Trade-off: Precision vs. Context Completeness".

Draw a 2x2 quadrant diagram with these axes:
- X-axis (horizontal): "Chunk Size (tokens)" from SMALL (left) to LARGE (right)
- Y-axis (vertical): "Quality" from LOW (bottom) to HIGH (top)

Plot TWO curves:
1. "Retrieval Precision" — downward sloping curve from top-left to bottom-right. Color: blue (#1565C0), line thick, solid. Annotated with "↑ Precise but loses context" near top-left.
2. "Context Completeness" — upward sloping curve from bottom-left to top-right. Color: orange (#E65100), line thick, solid. Annotated with "↑ Rich context but imprecise" near top-right.

The two curves INTERSECT at the center. Mark the intersection with a large gold star ★ and label it "Optimal chunk size ℓ*".

Add a shaded green vertical band around the intersection point labeled "Recommended zone".

Below the main chart, draw a small horizontal strip showing three example chunks:
- Left: tiny chunk box (labeled "chunk_size=50, overlap=10") with ✗ "loses context"
- Center: medium chunk box (labeled "chunk_size=350, overlap=70") with ✓ "balanced"  
- Right: large chunk box (labeled "chunk_size=1000, overlap=200") with ✗ "imprecise retrieval"

Style: Clean academic chart, white background, gray gridlines, legend in top center, sans-serif font, all labels in English. 1600x1000px.