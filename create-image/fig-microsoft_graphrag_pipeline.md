Create a professional academic pipeline diagram on a white background titled "Microsoft GraphRAG — Query Processing Pipeline".

The diagram has TWO parallel horizontal swimlanes:

TOP SWIMLANE (light blue background #E3F2FD, labeled "Local Query Mode" on the left):
Boxes in sequence left to right connected by solid arrows:
[Local Query] → [Entity Retrieval] → [Graph Traversal k-hop] → [Local Context]

BOTTOM SWIMLANE (light orange background #FFF3E0, labeled "Global Query Mode" on the left):
Boxes in sequence left to right connected by solid arrows:
[Global Query] → [Community Summaries] → [Map-Reduce Aggregation] → [Global Context]

Both swimlanes converge on the right side with arrows pointing to a shared box:
[LLM Generator] (red/pink fill #FFCDD2)
Then one final arrow from LLM Generator to:
[Answer] (green fill #C8E6C9)

All boxes are rounded rectangles with subtle drop shadow. Arrows are thick dark gray with arrowheads.

Add small icons inside each box:
- Entity Retrieval: magnifying glass icon
- Graph Traversal: network/graph icon  
- Community Summaries: cluster/bubble icon
- Map-Reduce: funnel icon
- LLM Generator: brain/AI chip icon

Add annotation: "Two parallel retrieval strategies converge at generation layer" as a caption below.

Style: Clean enterprise architecture diagram style, white background between swimlanes, bold title, sans-serif font, professional color palette. 1800x900px.
